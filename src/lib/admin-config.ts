/**
 * Admin credentials.
 *
 * To change the admin password, edit ADMIN_PASSWORD below and redeploy.
 * For production hardening, move this to an environment variable and validate
 * server-side via the /api/admin route (see TODO in src/routes/api/data.ts).
 */
export const ADMIN_USERNAME = "admin";
export const ADMIN_PASSWORD = "admin123";

export const ADMIN_SESSION_KEY = "mts_admin_session";
